export const environment = {
  production: false,
  forumApiUrl: 'http://localhost:8090',
  usersApiUrl: 'http://localhost:8087',
  /** Keycloak server URL */
  keycloakUrl: 'http://localhost:8080',
  /** When true, bypasses Keycloak auth for admin routes (dev only) */
  devBypassAuth: true
};
