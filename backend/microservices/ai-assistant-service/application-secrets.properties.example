# Copier ce fichier vers application-secrets.properties (même dossier que pom.xml)
# et renseigner la clé. Ce fichier est ignoré par Git.

gemini.api.key=
# Optionnel : surcharger le modèle
# gemini.api.model=gemini-1.5-flash
76