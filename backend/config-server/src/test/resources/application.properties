# Test-only defaults (classpath overrides main application.properties during tests).
spring.profiles.active=native,test
# When CI/Sonar passes only -Dspring.profiles.active=ci, also activate native (Boot 4 forbids spring.profiles.include in application-ci.properties).
spring.profiles.group.ci=native
