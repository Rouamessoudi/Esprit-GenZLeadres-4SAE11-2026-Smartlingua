/** URL du backend Messaging (auth, chat, presence, WebSocket). Une seule source de verite. */
export const MESSAGING_API_BASE = 'http://localhost:8094';

/** Microservice Assistant IA (Gemini). La clé API reste uniquement côté serveur. */
export const AI_API_BASE = 'http://localhost:8095';
